package com.github.kettoleon.ti.dashboard.view;

/**
 * I'm going to start first with simple Swing UI, but later on we might want to have a workspace like UI with another library.
 * Hopefully we can reuse some of the components, but the setup/initialisation will change.
 */
public interface DashboardView {

    void start();
}
